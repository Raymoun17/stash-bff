# AI product extraction

AI extraction is intentionally not implemented yet. The preview API accepts
`ai_fallback` and `ai_only` as preparatory modes, but both currently execute the
same deterministic extractor pipeline as `standard`.
